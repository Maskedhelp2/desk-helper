[dependencies]
tauri = { version = "2", features = [] }

hidapi = "2.6"

serde = { version = "1", features = ["derive"] }
serde_json = "1"

tokio = { version = "1", features = ["full"] }

image = "0.25"
